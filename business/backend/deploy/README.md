# 本地容器

完整说明见仓库根 [`docs/容器部署.md`](../../../docs/容器部署.md)。

本目录放 Gateway Compose。后端镜像在 `../Dockerfile`，Host 镜像在 `../../deploy/frontend.Dockerfile`。启动与停止使用 `../tools/up-local.ps1` 与 `../tools/down-local.ps1`。
